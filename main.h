#ifndef MAIN_H
#define MAIN_H

/* Library: */
#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
#include <stdarg.h>

/* Prototype: */
int _putchar(char c);
int _printf(const char *format, ...);

#endif
